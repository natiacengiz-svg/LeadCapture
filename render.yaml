services:
  - type: web
    name: ekol-medya-cast
    runtime: image
    repo: https://github.com/natiacengiz-svg/LeadCapture
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production

databases:
  - name: ekol-medya-db
    plan: free
    databaseName: leads
    user: admin
